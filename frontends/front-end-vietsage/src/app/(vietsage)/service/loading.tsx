import { ContentLoadingState } from "../_components/route-boundary-state";

export default function ServiceLoading() {
  return <ContentLoadingState label="Đang tải dữ liệu đối tác dịch vụ..." tone="staff" />;
}
