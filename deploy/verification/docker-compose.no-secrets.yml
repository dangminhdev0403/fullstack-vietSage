# Syntax-only override: removes runtime env files; never use for deployment.
services:
  postgres:
    env_file: !reset []
  migrate:
    env_file: !reset []
  auth-service:
    env_file: !reset []
  frontend:
    env_file: !reset []
