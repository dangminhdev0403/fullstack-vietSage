This file is a merged representation of a subset of the codebase, containing specifically included files, combined into a single document by Repomix.
The content has been processed where content has been compressed (code blocks are separated by ⋮---- delimiter).

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of a subset of the repository's contents that is considered the most important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Only files matching these patterns are included: frontends/front-end-vietsage/src/app/(vietsage)/owner/(global)/dashboard/page.tsx, frontends/front-end-vietsage/src/features/hotel-ops/utils/owner-attention-route.ts, frontends/front-end-vietsage/src/features/hotel-ops/utils/owner-attention-route.test.ts, frontends/front-end-vietsage/src/proxy.ts, services/auth-service/src/modules/property/application/hotel-dashboard.service.ts
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Content has been compressed - code blocks are separated by ⋮---- delimiter
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
frontends/
  front-end-vietsage/
    src/
      app/
        (vietsage)/
          owner/
            (global)/
              dashboard/
                page.tsx
      features/
        hotel-ops/
          utils/
            owner-attention-route.test.ts
            owner-attention-route.ts
      proxy.ts
services/
  auth-service/
    src/
      modules/
        property/
          application/
            hotel-dashboard.service.ts
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="frontends/front-end-vietsage/src/features/hotel-ops/utils/owner-attention-route.test.ts">
import assert from "node:assert/strict";
import test from "node:test";
⋮----
// @ts-expect-error Node's strip-types runner requires the explicit TypeScript extension.
import { ownerAttentionRoute } from "./owner-attention-route.ts";
</file>

<file path="frontends/front-end-vietsage/src/features/hotel-ops/utils/owner-attention-route.ts">
export function ownerAttentionRoute(route: string, hotelId: string): string
</file>

<file path="services/auth-service/src/modules/property/application/hotel-dashboard.service.ts">
import { Injectable } from "@nestjs/common";
import {
  FolioStatus,
  GuestRequestPriority,
  GuestRequestStatus,
  GuestStayStatus,
  PaymentStatus,
  Prisma,
  RoomStatus,
} from "@prisma/client";
import { PrismaService } from "../../../prisma/prisma.service";
import { HotelAccessService } from "./hotel-access.service";
⋮----
function toNumber(value: Prisma.Decimal | number | null | undefined): number
⋮----
function startOfDay(date: Date): Date
⋮----
function addDays(date: Date, days: number): Date
⋮----
function mapRoomStatus(
  status: RoomStatus,
): "available" | "occupied" | "processing" | "maintenance"
⋮----
function mapRequestStatus(
  status: GuestRequestStatus,
): "sent" | "processing" | "completed" | "cancelled"
⋮----
type RoomStatusSummary = {
  available: number;
  occupied: number;
  processing: number;
  maintenance: number;
};
⋮----
type DashboardInsight = {
  id: string;
  type: string;
  severity: "info" | "warning" | "critical";
  title: string;
  description: string;
  metric?: { current: number; previous?: number; changePercent?: number };
};
⋮----
function buildDashboardHealth(input: {
  urgentUnprocessedRequests: number;
  unprocessedRequests: number;
  roomsByStatus: RoomStatusSummary;
  pendingCheckOuts: number;
  failedPayments: number;
})
⋮----
function buildDashboardInsights(input: {
  todayRequests: number;
  yesterdayRequests: number;
  todayUrgentRequests: number;
  yesterdayUrgentRequests: number;
  topService?: { serviceItemId: string | null; count: number; name: string };
}): DashboardInsight[]
⋮----
function buildDashboardSla(
  completedRequests: Array<{ id: string; createdAt: Date; completedAt: Date | null }>,
  acknowledgedEvents: Array<{ requestId: string; _min: { createdAt: Date | null } }>,
)
⋮----
const average = (values: number[])
⋮----
export class HotelDashboardService {
⋮----
constructor(
⋮----
async getDashboard(actorUserId: string, activeRoleId: string, hotelId: string)
</file>

<file path="frontends/front-end-vietsage/src/app/(vietsage)/owner/(global)/dashboard/page.tsx">
import Link from "next/link";
import { notFound } from "next/navigation";
import type { ReactNode } from "react";
⋮----
import { auth } from "@/auth";
import { resolveWorkspacePersona } from "@/features/workspace/config/workspace-registry";
import { adminService } from "@/features/admin/service/admin-service-instance";
import { hotelOpsService } from "@/features/hotel-ops/service/hotel-ops-service-instance";
import { ownerAttentionRoute } from "@/features/hotel-ops/utils/owner-attention-route";
import { readServerSessionTokens } from "@/libs/server-session-tokens";
import { createAuthorizedApiExecutor } from "@/libs/server-api-auth";
import { loadServerWorkspaceContext } from "@/libs/server-workspace-context";
⋮----
import { VsIcon } from "../../../_components/vs-icon";
import { AnimatedDashboardNumber } from "./animated-dashboard-number";
⋮----
type Dashboard = {
  hotelId: string;
  generatedAt: string;
  rooms: {
    total: number;
    occupied: number;
    occupancyRate: number;
    byStatus: Record<string, number>;
  };
  stays: {
    todayCheckIns: number;
    todayCheckOuts: number;
    pendingCheckOuts: number;
    activeStays: number;
  };
  requests: {
    unprocessed: number;
    urgentUnprocessed: number | null;
    byStatus: Record<string, number>;
    topServices: Array<{ serviceName: string; count: number }>;
  };
  revenue: {
    available: boolean;
    currency: string;
    today: number | null;
    last7Days: number | null;
    currentMonth: number | null;
  };
  health: {
    score: number | null;
    status: "excellent" | "good" | "warning" | "critical" | "unknown";
    title: string;
    factors: Array<{
      type: string;
      label: string;
      impact: string;
      message: string;
    }>;
  };
  attention: Array<{
    id: string;
    type: string;
    priority: "urgent" | "high" | "normal";
    title: string;
    description: string;
    createdAt: string;
    action: { label: string; route: string };
  }>;
  insights: Array<{
    id: string;
    severity: "info" | "warning" | "critical";
    title: string;
    description: string;
    metric?: { current: number; previous?: number; changePercent?: number };
  }>;
  sla: {
    available: boolean;
    averageResponseMinutes: number | null;
    averageCompletionMinutes: number | null;
    completedWithinSlaPercent: number | null;
    thresholdMinutes: number;
  };
  activities: Array<{
    id: string;
    type: string;
    title: string;
    description: string;
    createdAt: string;
  }>;
  warnings: string[];
};
⋮----
function formatVnd(value: number | null | undefined): string
⋮----
function formatTime(value: string): string
⋮----
function healthTone(status: Dashboard["health"]["status"]): string
⋮----
function severityTone(severity: string): string
⋮----
function fallbackStatusLabel(status: string): string
⋮----
function roomStatusLabel(status: string): string
⋮----
function requestStatusLabel(status: string): string
⋮----
function insightSeverityLabel(severity: string): string
⋮----
href=
</file>

<file path="frontends/front-end-vietsage/src/proxy.ts">
import { type NextRequest, NextResponse } from "next/server";
⋮----
import { auth } from "./auth";
import { createRequestRedirectUrl } from "./features/auth/utils/redirect-isolation-core";
import { canAccessPathByRoles, getDefaultPathForRoles, sanitizeInternalCallbackUrl } from "./libs/rbac";
⋮----
function matchesPrefix(pathname: string, prefix: string): boolean
⋮----
function isForcedReauth(request: NextRequest): boolean
⋮----
function isNextAuthCookie(cookieName: string): boolean
⋮----
function clearNextAuthCookies(request: NextRequest, response: NextResponse): NextResponse
⋮----
function buildLoginRedirect(request: NextRequest): NextResponse
⋮----
// Prevent callbackUrl nesting growth when a protected URL already carries callbackUrl
⋮----
function getStringTokenField(token: unknown, field: string): string | null
⋮----
function getNumberTokenField(token: unknown, field: string): number | null
⋮----
function buildRefreshSessionRedirect(request: NextRequest): NextResponse
⋮----
// Cross-workspace guard: if the user's role cannot access this protected prefix,
// redirect to their correct dashboard instead of letting the layout return notFound().
</file>

</files>
